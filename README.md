# 🦷 Лендинг стоматологической клиники "Ультра-дент"

Адаптивный лендинг с функциональными блоками: таймер акции, система купонов, формы обратной связи и слайдер отзывов.

## 📋 Содержание

- [Технологии](#-технологии)
- [Требования](#-требования)
- [Установка](#-установка)
- [Запуск проекта](#-запуск-проекта)
- [Структура проекта](#-структура-проекта)
- [Функциональность](#-функциональность)
- [Конфигурация](#-конфигурация)
- [Сборка для production](#-сборка-для-production)

## 🛠 Технологии

- **HTML5** — семантическая разметка
- **SCSS** — препроцессор CSS
- **JavaScript (ES6+)** — модульная архитектура
- **Vite** — сборщик и dev-сервер

## ⚙️ Требования

Перед установкой убедитесь, что у вас установлены:

- **Node.js** версии 16.0.0 или выше
- **npm** версии 7.0.0 или выше (поставляется с Node.js)

Проверить версии можно командами:

```bash
node --version
npm --version
```

## 📦 Установка

### Шаг 1: Клонирование или распаковка проекта

Если проект в архиве, распакуйте его в удобную директорию.

### Шаг 2: Переход в директорию проекта

```bash
cd ultra-dent
```

### Шаг 3: Установка зависимостей

```bash
npm install
```

Эта команда установит все необходимые зависимости, указанные в `package.json`:
- **vite** — сборщик и dev-сервер
- **sass** — компилятор SCSS

## 🚀 Запуск проекта

### Режим разработки

Для запуска локального dev-сервера с hot-reload:

```bash
npm run dev
```

После выполнения команды:
1. Проект автоматически откроется в браузере по адресу `http://localhost:3000`
2. При изменении файлов страница будет автоматически обновляться
3. В терминале вы увидите сообщение:

```
  VITE v5.x.x  ready in xxx ms

  ➜  Local:   http://localhost:3000/
  ➜  Network: use --host to expose
```

### Остановка dev-сервера

Нажмите `Ctrl + C` в терминале.

## 📁 Структура проекта

```
ultra-dent/
├── index.html                 # Главная HTML страница
├── package.json              # Зависимости и скрипты
├── vite.config.js           # Конфигурация Vite
├── .gitignore               # Игнорируемые файлы
│
├── src/                     # Исходные файлы
│   ├── js/                  # JavaScript
│   │   ├── main.js         # Точка входа
│   │   ├── config.js       # Конфигурация акции и купонов
│   │   │
│   │   ├── modules/        # Модули приложения
│   │   │   ├── Timer.js           # Таймер акции
│   │   │   ├── Modal.js           # Модальные окна
│   │   │   ├── Form.js            # Форма заявки
│   │   │   ├── Slider.js          # Слайдер отзывов
│   │   │   ├── Quiz.js            # Тест для получения купона
│   │   │   ├── Coupons.js         # Управление купонами
│   │   │   └── MobileMenu.js      # Мобильное меню
│   │   │
│   │   └── utils/          # Утилиты
│   │       ├── Toast.js           # Всплывающие уведомления
│   │       └── Validator.js       # Валидация форм
│   │
│   └── styles/             # SCSS стили
│       ├── main.scss       # Главный файл стилей
│       │
│       ├── base/           # Базовые стили
│       │   ├── _variables.scss    # Переменные (цвета, размеры)
│       │   ├── _reset.scss        # Сброс стилей
│       │   ├── _typography.scss   # Типографика
│       │   └── _global.scss       # Глобальные стили
│       │
│       ├── components/     # Компоненты
│       │   ├── _button.scss       # Кнопки
│       │   ├── _modal.scss        # Модальные окна
│       │   ├── _form.scss         # Формы
│       │   └── _toast.scss        # Уведомления
│       │
│       └── blocks/         # Блоки страницы (БЭМ)
│           ├── _header.scss       # Шапка
│           ├── _hero.scss         # Главный экран
│           ├── _promo.scss        # Блок акций
│           ├── _timer.scss        # Таймер
│           ├── _coupons.scss      # Купоны
│           ├── _services.scss     # Услуги
│           ├── _reviews.scss      # Отзывы
│           ├── _slider.scss       # Слайдер
│           ├── _contacts.scss     # Контакты
│           ├── _footer.scss       # Подвал
│           └── _quiz.scss         # Тест
│
└── dist/                   # Продакшн сборка (создается при сборке)
```

## ✨ Функциональность

### 1. ⏱ Таймер акции

**Расположение:** Блок "Специальное предложение"

**Функции:**
- Отображает оставшееся время до окончания акции
- Обновляется каждую секунду
- Показывает дни, часы, минуты и секунды
- После завершения акции:
  - Выводит сообщение "Акция завершена"
  - Блокирует все кнопки записи и получения купона

**Конфигурация:** `src/js/config.js`

```javascript
export const config = {
  promo: {
    start: '2026-02-01T00:00:00',  // Начало акции
    end: '2026-02-28T23:59:59'     // Окончание акции
  },
  // ...
};
```

### 2. 🎫 Система купонов

**Расположение:** Блок "Специальное предложение"

**Функции:**
- Отображает количество оставшихся купонов
- Кнопка "Получить купон со скидкой" открывает мини-тест
- После прохождения теста:
  - Количество купонов уменьшается на 1
  - Данные сохраняются в `localStorage`
  - Показывается уведомление об успехе
  - Пользователь не может получить купон повторно

**Хранение данных:**
- `localStorage.coupons_remaining` — количество оставшихся купонов
- `localStorage.user_has_coupon` — флаг получения купона пользователем
- `localStorage.quiz_submissions` — ответы на тест

**Визуальные индикаторы:**
- При количестве ≤ 5 — жёлтый цвет и пульсация
- При количестве = 0 — красный цвет

### 3. 📝 Мини-тест для получения купона

**Вопросы:**

1. Как часто вы посещаете стоматолога?
   - Раз в год
   - Раз в месяц
   - Никогда

2. Есть ли у Вас импланты?
   - Да
   - Нет

3. Планируете лечение в ближайшие 3 месяца?
   - Да
   - Нет

**Особенности:**
- Один вариант ответа на вопрос (radio buttons)
- Навигация между вопросами
- Прогресс-бар
- Валидация (нельзя пропустить вопрос)

### 4. 📋 Форма обратной связи

**Поля формы:**
- Фамилия* (обязательное)
- Имя* (обязательное)
- Отчество
- Телефон* (обязательное, с маской +7 (___) ___-__-__)
- Email* (обязательное)

**Валидация:**
- Проверка обязательных полей
- Валидация формата email
- Валидация телефона (11 цифр, начинается с 7)
- Валидация ФИО (только буквы)
- Отображение ошибок под полями

**После отправки:**
- Имитация отправки (задержка 1 секунда)
- Сохранение данных в `localStorage.appointments`
- Успешное уведомление в правом верхнем углу
- Закрытие модального окна
- Очистка формы

### 5. 💬 Слайдер отзывов

**Источник данных:**
```
https://raw.githubusercontent.com/axelwarn2/frontend-test-data/refs/heads/main/reviews.json
```

**Функции:**
- Загрузка отзывов с внешнего API
- Кастомный слайдер без библиотек
- Адаптивность:
  - Desktop (≥1200px): 3 слайда
  - Tablet (768-1199px): 2 слайда
  - Mobile (<768px): 1 слайд
- Навигация:
  - Кнопки "вперед" / "назад"
  - Точки пагинации
  - Свайп на мобильных устройствах
  - Перетаскивание мышью на десктопе

**Формат данных отзыва:**
```json
{
  "name": "Иван Иванов",
  "rating": 5,
  "text": "Текст отзыва",
  "date": "2024-01-15"
}
```

### 6. 📱 Адаптивная верстка

**Брейкпоинты:**
- Mobile: до 480px
- Tablet: 768px
- Laptop: 1024px
- Desktop: 1200px

**Адаптивные элементы:**
- Мобильное меню (бургер)
- Гибкие сетки
- Масштабирование шрифтов
- Оптимизация отступов

### 7. 🔔 Система уведомлений (Toast)

**Типы уведомлений:**
- Success (зелёный) — успешные операции
- Error (красный) — ошибки
- Info (синий) — информация
- Warning (жёлтый) — предупреждения

**Особенности:**
- Появляются в правом верхнем углу
- Автоматически исчезают через 5 секунд
- Можно закрыть вручную
- Анимация появления/исчезновения
- Адаптивны на мобильных устройствах

## ⚙️ Конфигурация

### Изменение параметров акции

Откройте файл `src/js/config.js`:

```javascript
export const config = {
  promo: {
    start: '2026-02-01T00:00:00',  // Дата начала акции
    end: '2026-02-28T23:59:59'     // Дата окончания акции
  },
  coupons: {
    total: 35  // Начальное количество купонов
  }
};
```

**Формат даты:** `YYYY-MM-DDTHH:MM:SS`

**Примеры:**
- `'2026-03-01T00:00:00'` — 1 марта 2026 года, 00:00
- `'2026-12-31T23:59:59'` — 31 декабря 2026 года, 23:59

### Сброс данных пользователя

Для сброса данных пользователя (купоны, заявки) откройте консоль браузера (F12) и выполните:

```javascript
// Сбросить купоны
localStorage.removeItem('coupons_remaining');
localStorage.removeItem('user_has_coupon');
localStorage.removeItem('quiz_submissions');

// Сбросить заявки
localStorage.removeItem('appointments');

// Полная очистка
localStorage.clear();
```

Затем обновите страницу.

## 🏗 Сборка для production

### Создание production-сборки

```bash
npm run build
```

После выполнения команды:
1. В корне проекта появится папка `dist/`
2. В ней будут оптимизированные файлы:
   - Минифицированный HTML
   - Объединённый и минифицированный CSS
   - Объединённый и минифицированный JavaScript
   - Оптимизированные ресурсы

### Предпросмотр production-сборки

```bash
npm run preview
```

Откроет локальный сервер на `http://localhost:4173` с production-версией.

### Деплой

Содержимое папки `dist/` готово к деплою на любой статический хостинг:
- Netlify
- Vercel
- GitHub Pages
- AWS S3
- И другие

**Простой способ — GitHub Pages:**

1. Инициализируйте git (если ещё не сделано):
```bash
git init
git add .
git commit -m "Initial commit"
```

2. Создайте репозиторий на GitHub

3. Добавьте удалённый репозиторий:
```bash
git remote add origin https://github.com/username/repository.git
git push -u origin main
```

4. Соберите проект:
```bash
npm run build
```

5. Опубликуйте на GitHub Pages:
```bash
npm install -g gh-pages
gh-pages -d dist
```

Сайт будет доступен по адресу: `https://username.github.io/repository`

## 📝 Скрипты package.json

```json
{
  "scripts": {
    "dev": "vite",              // Запуск dev-сервера
    "build": "vite build",      // Сборка для production
    "preview": "vite preview"   // Предпросмотр production-сборки
  }
}
```

## 🔧 Возможные проблемы и решения

### Проблема: Ошибка при `npm install`

**Решение:**
1. Удалите `node_modules` и `package-lock.json`:
```bash
rm -rf node_modules package-lock.json
```

2. Очистите кэш npm:
```bash
npm cache clean --force
```

3. Установите зависимости заново:
```bash
npm install
```

### Проблема: Порт 3000 уже занят

**Решение:**

Измените порт в `vite.config.js`:

```javascript
export default defineConfig({
  server: {
    port: 3001, // Измените на другой порт
    open: true
  }
});
```

### Проблема: Страница не обновляется автоматически

**Решение:**

1. Проверьте, что dev-сервер запущен (`npm run dev`)
2. Попробуйте жёсткое обновление: `Ctrl + Shift + R` (Windows) или `Cmd + Shift + R` (Mac)
3. Очистите кэш браузера

### Проблема: Не загружаются отзывы

**Решение:**

1. Проверьте подключение к интернету
2. Откройте консоль браузера (F12) и проверьте ошибки
3. Убедитесь, что URL отзывов доступен:
```
https://raw.githubusercontent.com/axelwarn2/frontend-test-data/refs/heads/main/reviews.json
```

## 📚 Методология БЭМ

Проект использует методологию БЭМ (Блок, Элемент, Модификатор) для именования классов:

**Примеры:**

```html
<!-- Блок -->
<div class="timer">
  <!-- Элемент -->
  <div class="timer__item">
    <!-- Элемент с модификатором -->
    <span class="timer__value timer__value--highlighted">00</span>
  </div>
</div>
```

**Правила:**
- Блок: `.block`
- Элемент: `.block__element`
- Модификатор: `.block__element--modifier`

## 🎨 Кастомизация

### Изменение цветов

Откройте `src/styles/base/_variables.scss`:

```scss
// Основные цвета
$primary: #0066cc;        // Основной цвет (синий)
$accent: #ff6b35;         // Акцентный цвет (оранжевый)

// Цвета статусов
$success: #28a745;        // Успех (зелёный)
$error: #dc3545;          // Ошибка (красный)
$warning: #ffc107;        // Предупреждение (жёлтый)
```

### Изменение шрифтов

Откройте `src/styles/base/_variables.scss`:

```scss
$font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', ...;
$font-size-base: 16px;
```

Для использования Google Fonts добавьте в `index.html`:

```html
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap" rel="stylesheet">
```

И измените переменную:

```scss
$font-family: 'Roboto', sans-serif;
```

## 📄 Лицензия

Проект создан в учебных целях.

## 👨‍💻 Автор

Разработано как тестовое задание для позиции Junior Frontend Developer.

---

**Успешной работы с проектом! 🚀**

Если возникнут вопросы, проверьте:
1. Версии Node.js и npm
2. Установлены ли зависимости (`node_modules/`)
3. Запущен ли dev-сервер
4. Консоль браузера на наличие ошибок
# ultradent_without_framework
